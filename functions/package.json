{
  "name": "tasteful-panthers-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.1.0",
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "eslint": "^9.10.0",
    "typescript": "^5.6.3"
  }
}
